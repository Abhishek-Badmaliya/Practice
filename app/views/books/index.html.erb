<h1 class="current-category"><%= params[:category] %></h1>

<% if @books.count == 0 %>
  <h4>Hey, currently there are no books available in this category!</h4>
<% else %>
  <% @books.each do |book| %>
    <h2><%= link_to book.book_title, book_path(book) %></h2>
  <%end%>
<%end%>
